[project]
name = "formio-chat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "pandas>=3.0.5",
    "python-dotenv>=1.0.0",
    "google-genai>=2.21.0",
]

[project.scripts]
formio-chat = "formio_chat:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
